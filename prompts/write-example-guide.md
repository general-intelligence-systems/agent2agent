The ./examples have README.md files explaining how to run the exmaples.... **your mission** is to deliver the knowledge in these examples as the word of god... the ONLY way to do this is to make them a step-by-step guide to running the example... do not give different options for running the server... only use docker-compose in examples... give instructions on starting docker compose, checking the logs... runnign commands... giving the expected results at each step..
